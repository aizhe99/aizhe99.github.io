baseURL: "https://aizhe99.github.io/"
languageCode: zh-cn
title: 周记
theme: PaperMod

enableRobotsTXT: true
buildDrafts: false
buildFuture: false
buildExpired: false

# PaperMod 核心参数
params:
  env: production
  title: 周记
  description: "一次性人生"
  keywords: [Blog, Tech, Life]
  author: Me
  
  # 默认主题模式 (auto/dark/light)
  defaultTheme: auto
  
  # 首页显示的个人信息模块 (Home Info Mode)
  # 已注释掉，以确保首页直接显示文章列表
  # homeInfoParams:
  #   Title: "你好 👋"
  #   Content: "欢迎来到我的 PaperMod 博客"

  # 底部版权信息
  copyright: "一次性人生"

  # 社交图标配置
  # 修改为仅显示 RSS 订阅按钮
  socialIcons:
    - name: rss
      url: "https://aizhe99.github.io/index.xml"

# 菜单配置
menu:
  main:
    - identifier: categories
      name: 分类
      url: /categories/
      weight: 10
    - identifier: tags
      name: 标签
      url: /tags/
      weight: 20
    - identifier: archives
      name: 归档
      url: /archives/
      weight: 30
